# Для списка реализовать обмен значений соседних элементов, т.е.
# Значениями обмениваются элементы с индексами 0 и 1, 2 и 3 и т.д.
# При нечетном количестве элементов последний сохранить на своем месте.
# Для заполнения списка элементов необходимо использовать функцию input().
clear_list = []
for i in range(int(input('Введите кол-во элементов в списке: '))):
    clear_list.append(int(input('Введите целое число: ')))
    print(clear_list)
list = clear_list
print('До обмена значений список -', list)
value = 0
for i in range(int(len(list)/2)):
    list[value], list[value + 1] = list[value + 1], list[value]
    value += 2
print('После обмена значений список -', list)
